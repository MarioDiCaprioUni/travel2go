import {AppBar, styled} from "@mui/material";


export const Context = styled(AppBar)`
  box-shadow: none;
  background: transparent;
  backdrop-filter: blur(15px);
  margin-bottom: 50px;
`;

export const Logo = styled('div')`
  display: flex;
  flex-grow: 1;
`;